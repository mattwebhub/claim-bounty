# Peer2Paper Product Package

> **Status**: In Development | **Updated**: 2026-08-31 | **Scope**: Peer2Paper public product-package checklist

## Present in this projection

- Hosted web, API, storage, scanning, administration, worker, and export surfaces.
- Public OpenAPI and data contracts.
- Parent scientific-audit routine, six child routines, schemas, templates, and local runner.
- Reviewer and evidence packages with deterministic SHA-256 manifests.
- Verified CLI session, token, runtime, and monetary-cost disclosure with machine-readable evidence.
- Cited human-effort estimate with eight work packages and machine-readable evidence.
- Sealed public-safe one-shot ChatGPT comparator record with terminal classification, Alpha and Beta scores, and final adjudication.
- Public-safe Peer2Paper exploratory-attempt record with elapsed time, stage outcomes, blocked gates, and two independent qualification deviations.
- Setup, architecture, protocol, limitations, provenance, and demonstration documentation.
- Public-release checks for private paths, risky package contents, link drift, and manifest drift.
- Included CC BY 4.0 Moura et al. 2023 Heliconius Experiment 1 paper, R script, and data bundle.
- Screened 4:54 H.264 product walkthrough at `submission/recordings/peer2paper-product-walkthrough.mp4`.

## Pending before submission

- Treat the protocol-deviating Peer2Paper attempt as unscored; do not promote it into a benchmark result.
- If a qualified comparison is pursued, remove the routine contamination, rerun from the byte-identical frozen prompt contract, complete executable reproduction and all required stages, then seal, independently review, adjudicate, and clear the public evidence subset.
- Verify a clean local workflow import and full run.
- Verify the `micro1/ClaimBounty` Explore Workflows listing and clean import against the release build.
- Confirm deployment and repository URLs, license choice, and final competition metadata.
- Confirm the fox-and-loupe source account-generation statement.
- Decide the official preexisting-work cutoff and approve research redistribution wording.

Submission documents may state the comparator's adjudicated 1/100 `invalid_incomplete` score with its missing-work context. They must not state a Peer2Paper quality score, a qualified head-to-head result, a measured human comparison, a speedup result, or public workflow-directory availability.

The case freeze is complete and validated. The ChatGPT comparator ended `truncated_or_malformed_output`; its required reviewer report and all eight requested sections were absent. Alpha scored it 1/100, Beta 2/100, and adjudication set 1/100 with no cap and decision `invalid_incomplete`.

The Peer2Paper exploratory attempt used one parent invocation and no external restart, elapsed 3799.936556 seconds, ended build `case_frozen_with_limits` and reproduction `not_executable`, ran zero scientific commands and repairs, and skipped five downstream stages. Qualification is blocked by two independent deviations: its dispatched 1,275-byte task contract omitted the frozen 1,276-byte prompt file's final LF byte, and the comparison routine injected two case-specific values from outside the frozen participant bundle. Those values were absent from the frozen prompt and every participant file and are omitted from the public projection. The latter deviation is out-of-contract contamination. Gates G2, G3, and G4 are `BLOCKED`; the attempt remains exploratory and unscored and cannot support a qualified comparison or speedup.

The challenge recommends 10 or more evaluation cases when feasible. The deadline-constrained public evidence contains one frozen Heliconius case because the full workflow takes multiple hours and each case requires preparation, licence review, freeze validation, and isolated execution. These outcomes cannot support general performance, human-quality superiority, or broad speedup claims.

The public usage disclosure reports the scientific workflow wall time separately from the parent-thread envelope. The [human baseline](HUMAN_BASELINE.md) is a 56-hour active-effort estimate for one qualified human, not observed duration or a quality score. Monetary cost remains unmeasured and must not be presented as zero or calculated from current API prices.
