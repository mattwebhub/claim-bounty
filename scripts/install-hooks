#!/bin/sh
set -eu

case "${CI:-}" in
  1 | true | TRUE | yes | YES)
    printf 'hooks: skipped in CI\n'
    exit 0
    ;;
esac

if ! git rev-parse --git-dir >/dev/null 2>&1; then
  printf 'hooks: skipped outside a Git worktree\n'
  exit 0
fi

pnpm exec lefthook install
