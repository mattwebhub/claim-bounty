apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: grader-deny-all
  namespace: peer2paper
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: grader
  policyTypes:
    - Ingress
    - Egress
